# Our Cookbook — Version 2

This update adds a burnt-orange theme, Wisconsin cutting-board hero artwork,
warmer cookbook-style typography, category chips, category-specific recipe
card colors, updated recipe detail styling, and a mobile-first layout.

Recipes still live in `recipes.js`.

Category colors live near the top of `app.js` in `CATEGORY_COLORS`.
